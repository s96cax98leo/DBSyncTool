/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Output: 'export' for static sites, or 'standalone' for server.
  // Default is fine for now.
  // output: 'standalone',
};

module.exports = nextConfig;
